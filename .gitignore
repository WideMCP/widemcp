# Dependencies
node_modules/
dist/

# Agent scratchpad — never commit
.agents/scratchpad/

# Task folders — never commit
.agents/tasks/

# Canonical agent workflow doc (local-only)
packages/AGENTS.md

# Cursor steering rules (local-only)
.cursor/rules

# Environment files — never commit
.env
.env.local

# Mac
.DS_Store

# Build
*.js.map
*.d.ts.map