{
  "name": "widemcp",
  "version": "1.0.0",
  "description": "Production-grade open source MCP infrastructure. Quality workflow: docs/dev-quality.md",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "eslint packages",
    "lint:fix": "eslint packages --fix",
    "ci": "npm run lint && npm run build && npm run test && npm audit --audit-level=high"
  },
  "author": "widemcp",
  "license": "Apache-2.0",
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "eslint": "^9.25.0",
    "globals": "^16.0.0",
    "typescript-eslint": "^8.31.0"
  }
}